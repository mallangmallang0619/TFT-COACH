# Screen capture
mss>=9.0.0
windows-capture==2.0.0; platform_system == "Windows"

# Computer vision
opencv-python>=4.9.0
numpy>=1.26.0

# OCR
pytesseract>=0.3.10
Pillow>=10.0.0

# WebSocket server
websockets>=12.0

# Window management (cross-platform)
pygetwindow>=0.0.9    # Windows
# For Linux: xdotool (system package)
# For macOS: pyobjc-framework-Quartz

# Utilities
pydantic>=2.5.0

# Unit classifier inference — needed once a trained model ships in
# assets/models/ (harmless to install early; without it the classifier
# is silently disabled and the roster fallback is used):
# onnxruntime>=1.20

# Training only (developers, never end users) — see scripts/train_classifier.py:
# pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
